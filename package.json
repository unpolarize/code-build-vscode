{
  "name": "code-build-vscode",
  "displayName": "Code Build",
  "description": "A generalized, Claude-Code-style coding agent UI that drives Claude, Grok, Codex and any ACP agent behind one chat.",
  "version": "0.19.3",
  "publisher": "zhirafovod",
  "author": {
    "name": "Sergey Sergeev",
    "email": "zhirafovod@gmail.com",
    "url": "https://github.com/unpolarize"
  },
  "license": "MIT",
  "icon": "media/icon.png",
  "homepage": "https://github.com/unpolarize/code-build-vscode#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/unpolarize/code-build-vscode.git"
  },
  "bugs": {
    "url": "https://github.com/unpolarize/code-build-vscode/issues"
  },
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "AI",
    "Chat",
    "Programming Languages"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onWebviewPanel:codeBuild.chat",
    "onView:codeBuild.liveMemory",
    "onCommand:codeBuild.newConversation",
    "onCommand:codeBuild.openInNewTab",
    "onCommand:codeBuild.openInNewWindow"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "codeBuild",
          "title": "Code Build",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "codeBuild": [
        {
          "id": "codeBuild.sidebar",
          "name": "Chat",
          "type": "webview"
        },
        {
          "id": "codeBuild.liveMemory",
          "name": "Live Memory",
          "type": "tree",
          "contextualTitle": "Memory entries the active agent will read",
          "icon": "$(database)"
        }
      ]
    },
    "commands": [
      {
        "command": "codeBuild.newConversation",
        "title": "Code Build: New Conversation",
        "icon": "$(sparkle)"
      },
      {
        "command": "codeBuild.handoff",
        "title": "Code Build: Handoff Pack + Continue on…"
      },
      {
        "command": "codeBuild.memory.refresh",
        "title": "Refresh",
        "icon": "$(refresh)",
        "category": "Code Build / Memory"
      },
      {
        "command": "codeBuild.memory.pin",
        "title": "Pin to context",
        "category": "Code Build / Memory"
      },
      {
        "command": "codeBuild.memory.unpin",
        "title": "Unpin",
        "category": "Code Build / Memory"
      },
      {
        "command": "codeBuild.memory.hide",
        "title": "Hide from context",
        "category": "Code Build / Memory"
      },
      {
        "command": "codeBuild.memory.unhide",
        "title": "Unhide",
        "category": "Code Build / Memory"
      },
      {
        "command": "codeBuild.memory.edit",
        "title": "Edit in source file",
        "category": "Code Build / Memory"
      },
      {
        "command": "codeBuild.memory.delete",
        "title": "Delete from source",
        "category": "Code Build / Memory"
      },
      {
        "command": "codeBuild.openInNewTab",
        "title": "Code Build: Open in New Tab",
        "icon": "$(split-horizontal)"
      },
      {
        "command": "codeBuild.openInNewWindow",
        "title": "Code Build: Open in New Window",
        "icon": "$(multiple-windows)"
      },
      {
        "command": "codeBuild.focusInput",
        "title": "Code Build: Focus Input"
      },
      {
        "command": "codeBuild.openInCodeSessions",
        "title": "Code Build: Open Session in Code Sessions"
      },
      {
        "command": "codeBuild.openPreviousSession",
        "title": "Code Build: Open Previous Conversation...",
        "icon": "$(history)"
      },
      {
        "command": "codeBuild.togglePerfPanel",
        "title": "Code Build: Toggle Session Performance Panel"
      },
      {
        "command": "codeBuild.copyPerfReport",
        "title": "Code Build: Copy Performance Flight Report"
      },
      {
        "command": "codeBuild.exportPerf",
        "title": "Code Build: Export Performance JSON"
      },
      {
        "command": "codeBuild.exportConversation",
        "title": "Code Build: Export Conversation…"
      },
      {
        "command": "codeBuild.showFlightRecorder",
        "title": "Code Build: Show Flight Recorder Output"
      },
      {
        "command": "codeBuild.voice.toggleDictation",
        "title": "Code Build: Toggle Voice Dictation",
        "icon": "$(mic)"
      },
      {
        "command": "codeBuild.voice.toggleInteractive",
        "title": "Code Build: Toggle Hands-Free Voice Mode",
        "icon": "$(unmute)"
      },
      {
        "command": "codeBuild.voice.startVis",
        "title": "Code Build: Start Voice Ideation Session",
        "icon": "$(comment-discussion)"
      },
      {
        "command": "codeBuild.voice.endVis",
        "title": "Code Build: End Voice Ideation Session (save to KP)"
      },
      {
        "command": "codeBuild.voice.stop",
        "title": "Code Build: Stop Voice"
      }
    ],
    "keybindings": [
      {
        "command": "codeBuild.newConversation",
        "key": "cmd+n",
        "mac": "cmd+n",
        "win": "ctrl+n",
        "when": "activeWebviewPanelId == codeBuild.chat"
      },
      {
        "command": "codeBuild.openInNewTab",
        "key": "cmd+shift+escape",
        "mac": "cmd+shift+escape",
        "win": "ctrl+shift+escape"
      },
      {
        "command": "codeBuild.focusInput",
        "key": "cmd+escape",
        "mac": "cmd+escape",
        "win": "ctrl+escape",
        "when": "activeWebviewPanelId == codeBuild.chat"
      },
      {
        "command": "codeBuild.newConversation",
        "key": "ctrl+alt+shift+c",
        "mac": "ctrl+shift+cmd+c"
      },
      {
        "command": "codeBuild.voice.toggleDictation",
        "key": "ctrl+shift+space",
        "mac": "ctrl+shift+space",
        "when": "activeWebviewPanelId == codeBuild.chat || view == codeBuild.sidebar"
      },
      {
        "command": "codeBuild.voice.toggleInteractive",
        "key": "ctrl+alt+shift+v",
        "mac": "ctrl+alt+shift+v"
      },
      {
        "command": "codeBuild.voice.startVis",
        "key": "ctrl+alt+shift+i",
        "mac": "ctrl+alt+shift+i"
      },
      {
        "command": "codeBuild.voice.stop",
        "key": "escape escape",
        "mac": "escape escape",
        "when": "false"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "codeBuild.newConversation",
          "group": "navigation@100"
        }
      ],
      "view/title": [
        {
          "command": "codeBuild.newConversation",
          "group": "navigation@1",
          "when": "view == codeBuild.sidebar"
        },
        {
          "command": "codeBuild.openPreviousSession",
          "group": "navigation@2",
          "when": "view == codeBuild.sidebar"
        },
        {
          "command": "codeBuild.openInNewTab",
          "group": "navigation@3",
          "when": "view == codeBuild.sidebar"
        },
        {
          "command": "codeBuild.memory.refresh",
          "when": "view == codeBuild.liveMemory",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "codeBuild.memory.pin",
          "when": "view == codeBuild.liveMemory && viewItem =~ /^memoryEntry($|\\.hidden$)/",
          "group": "1_pin"
        },
        {
          "command": "codeBuild.memory.unpin",
          "when": "view == codeBuild.liveMemory && viewItem =~ /\\.pinned/",
          "group": "1_pin"
        },
        {
          "command": "codeBuild.memory.hide",
          "when": "view == codeBuild.liveMemory && viewItem =~ /^memoryEntry($|\\.pinned$)/",
          "group": "2_hide"
        },
        {
          "command": "codeBuild.memory.unhide",
          "when": "view == codeBuild.liveMemory && viewItem =~ /\\.hidden/",
          "group": "2_hide"
        },
        {
          "command": "codeBuild.memory.edit",
          "when": "view == codeBuild.liveMemory && viewItem =~ /^memoryEntry/",
          "group": "3_edit"
        },
        {
          "command": "codeBuild.memory.delete",
          "when": "view == codeBuild.liveMemory && viewItem =~ /^memoryEntry/",
          "group": "9_destructive"
        }
      ],
      "webviewPanel/title": [
        {
          "command": "codeBuild.newConversation",
          "group": "navigation@1",
          "when": "activeWebviewPanelId == codeBuild.chat"
        },
        {
          "command": "codeBuild.openPreviousSession",
          "group": "navigation@2",
          "when": "activeWebviewPanelId == codeBuild.chat"
        },
        {
          "command": "codeBuild.openInNewTab",
          "group": "navigation@3",
          "when": "activeWebviewPanelId == codeBuild.chat"
        },
        {
          "command": "codeBuild.openInNewWindow",
          "group": "navigation@4",
          "when": "activeWebviewPanelId == codeBuild.chat"
        },
        {
          "command": "codeBuild.exportConversation",
          "group": "1_export@1",
          "when": "activeWebviewPanelId == codeBuild.chat"
        }
      ],
      "commandPalette": [
        {
          "command": "codeBuild.exportConversation",
          "when": "activeWebviewPanelId == codeBuild.chat"
        }
      ]
    },
    "configuration": {
      "title": "Code Build",
      "properties": {
        "codeBuild.defaultBackend": {
          "type": "string",
          "enum": [
            "claude",
            "grok",
            "codex",
            "opencode",
            "cline"
          ],
          "default": "claude",
          "description": "Default agentic CLI backend for new conversations."
        },
        "codeBuild.initialPermissionMode": {
          "type": "string",
          "enum": [
            "default",
            "plan",
            "acceptEdits",
            "bypass"
          ],
          "default": "bypass",
          "description": "Initial permission mode for new conversations. Defaults to 'bypass' — the agent runs autonomously without per-action approval prompts (Claude Code's --dangerously-skip-permissions / terminal 'YOLO' workflow). Requires codeBuild.allowDangerouslySkipPermissions to stay enabled, otherwise bypass is downgraded to 'default' at session start. Choose 'default', 'plan', or 'acceptEdits' for an approval-based flow."
        },
        "codeBuild.allowDangerouslySkipPermissions": {
          "type": "boolean",
          "default": true,
          "description": "Permit the 'bypass' permission mode, which runs the agent with no confirmation prompts. Enabled by default so new conversations start in autonomous (bypass) mode. SECURITY-SENSITIVE: while on, the agent can read/write files and run commands without asking — and in bypass mode your home directory is trusted by default (see codeBuild.additionalTrustedDirs). Turn this off to disable bypass and fall back to approval-based modes."
        },
        "codeBuild.showActiveQuestionBanner": {
          "type": "boolean",
          "default": true,
          "description": "Show a sticky banner under the chat header surfacing the user's current (or most recent) question. Helps keep the prompt visible while a long reply scrolls — matches Claude Code's pattern. Banner has an inline × to dismiss for the current session; this setting controls the global on/off."
        },
        "codeBuild.classifyTurns": {
          "type": "boolean",
          "default": false,
          "description": "After each end-of-turn, run a one-shot classification call on the active backend to label the turn with 1-3 topic chips. Off by default because each call costs a small inference (Haiku tier on claude). Only claude is wired today; grok one-shot mode pending."
        },
        "codeBuild.classifyModel": {
          "type": "string",
          "default": "haiku",
          "description": "Model alias passed to the classifier one-shot. 'haiku' is the cheap tier; 'sonnet' or 'opus' work too but rack up cost faster."
        },
        "codeBuild.additionalTrustedDirs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Extra directories the agent's tools are allowed to read/write beyond the workspace folder (passed to claude as --add-dir, mirrored for ACP backends in bypass mode). Without this, even 'bypass' permission mode keeps the agent scoped to the workspace. Leave empty to use defaults: in bypass mode the user's $HOME is automatically trusted (matches terminal-claude behaviour); in other modes only the workspace is in scope."
        },
        "codeBuild.binPaths": {
          "type": "object",
          "default": {},
          "description": "Optional explicit paths to backend CLIs, keyed by backend id (claude, grok, codex, opencode, cline)."
        },
        "codeBuild.autoStartSession": {
          "type": "boolean",
          "default": true,
          "description": "Connect to the default backend as soon as a *new empty* chat opens. Restored chats (VS Code reload / sidebar remount) load the transcript idle and only spawn on the first prompt, so old work does not look like it just started."
        },
        "codeBuild.stallWarnSeconds": {
          "type": "number",
          "default": 45,
          "minimum": 0,
          "description": "Seconds of total silence (no agent output and no tokens) during a turn before CB warns that the agent may be stuck and offers a Stop. Catches the intermittent claude CLI 'error_during_execution' stall that leaves the 'working…' spinner frozen. Set to 0 to disable the stall watchdog entirely."
        },
        "codeBuild.stallAutoCancelSeconds": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "description": "Seconds of total silence before CB auto-stops a stuck turn. Default 0 = warn-only (alert, never kill). The chat header picker overrides this per session (never / 2m / 5m / 10m). Suppressed while a tool/command is running. Must exceed stallWarnSeconds when set."
        },
        "codeBuild.governor.mode": {
          "type": "string",
          "enum": ["off", "warn", "hard"],
          "default": "warn",
          "description": "Stop governor: what happens when a session crosses a budget (tool calls / active time / estimated spend). 'warn' (default) shows a sticky banner and records the event but never interrupts; 'hard' also cancels the active stream (the session stays resumable); 'off' disables all budgets. Each budget fires at most once per session."
        },
        "codeBuild.governor.maxToolCalls": {
          "type": "number",
          "default": 400,
          "minimum": 0,
          "description": "Stop governor: total tool calls allowed per session. 0 disables this budget. The high default only catches genuine runaway loops."
        },
        "codeBuild.governor.maxWallMinutes": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "description": "Stop governor: minutes of ACTIVE agent time per session (turn-open wall clock, summed; idle time between your messages never counts). 0 (default) disables this budget."
        },
        "codeBuild.governor.maxEstUsd": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "description": "Stop governor: estimated USD spend per session, from the backend's cumulative usage cost. 0 (default) disables this budget."
        },
        "codeBuild.governor.dupToolStop": {
          "type": "boolean",
          "default": false,
          "description": "Stop governor: reserved — duplicate tool-signature loop detector (identical tool+args N times in a row). Not yet active; the detector ships as a follow-up slice."
        },
        "codeBuild.governor.noProgressStop": {
          "type": "boolean",
          "default": false,
          "description": "Stop governor: reserved — no-progress detector (subgoals unchanged across steps). Not yet active; the detector ships as a follow-up slice."
        },
        "codeBuild.defaultModel": {
          "type": "string",
          "default": "",
          "description": "Default model id passed to the backend on session start. Examples: claude-opus-4-7, claude-sonnet-4-6, grok-build, grok-4.20, gpt-5. Empty = let the backend pick. Can be overridden per-session from the Model dropdown in the chat header."
        },
        "codeBuild.defaultEffort": {
          "type": "string",
          "enum": [
            "default",
            "low",
            "medium",
            "high",
            "xhigh",
            "max"
          ],
          "default": "default",
          "description": "Default effort / thinking-budget level. Claude Code and Grok map this to --effort; Codex (o-series) maps it to --reasoning-effort. 'default' = let the backend pick."
        },
        "codeBuild.perfDebug": {
          "type": "string",
          "enum": [
            "off",
            "hud",
            "full"
          ],
          "default": "hud",
          "description": "Session performance debug. 'off' = no HUD/sampling. 'hud' = header chip (TTFT · host tax · ev/s · paint) + activity strip while busy + /perf panel. 'full' = also appends a flight report to the 'Code Build: Flight Recorder' output channel after every turn. Use this to separate model latency from CB host/webview tax."
        },
        "codeBuild.mcpServers": {
          "type": "array",
          "default": [],
          "description": "MCP servers passed to ACP backends (Grok, OpenCode, Cline) on session/new. Each item: { name, command, args? }. When this setting is left unset, Code Build injects the default personal-browser stack (chrome-devtools --autoConnect + playwright) so agents can use the live Chrome profile via CDP. Set explicitly to [] to pass no MCP servers (disables default injection). Prefer codeBuild.disableDefaultMcpServers for a discoverable off switch.",
          "items": {
            "type": "object",
            "required": [
              "name",
              "command"
            ],
            "properties": {
              "name": {
                "type": "string"
              },
              "command": {
                "type": "string"
              },
              "args": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "schemaTokens": {
                "type": "number",
                "minimum": 0,
                "description": "Optional host-only schema-token override for the MCP budget knapsack. Not sent to the ACP backend."
              }
            }
          }
        },
        "codeBuild.disableDefaultMcpServers": {
          "type": "boolean",
          "default": false,
          "description": "When true, do not inject the default chrome-devtools + playwright MCP servers on ACP session/new. Explicit codeBuild.mcpServers entries (if set) are still passed through; with no override, an empty list is sent (no default npx spawns)."
        },
        "codeBuild.mcpSchemaTokenBudget": {
          "type": "number",
          "default": 6000,
          "minimum": 0,
          "description": "Max estimated tool-schema tokens to inject on ACP session/new|load. Default stack costs: chrome-devtools ≈ 5811, playwright ≈ 4617. At the default 6000, playwright is deferred so the session starts under budget. Set to 0 to disable the filter and pass every resolved server (restore-all). Unknown-cost servers are included fail-open. The kp MCP entry is always exempt. See the 'Code Build: MCP' output channel for the included/deferred log line."
        },
        "codeBuild.mcpPriority": {
          "type": "array",
          "default": [],
          "items": {
            "type": "string"
          },
          "description": "Preferred MCP server names (highest priority first) when codeBuild.mcpSchemaTokenBudget is > 0. Servers not listed keep their relative mcpServers / default order after the priority list. Higher priority is never dropped to fit a lower one. Empty = use mcpServers array order, else the default browser stack order."
        },
        "codeBuild.kpMcp.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Inject the knowledge-planning MCP server (kp mcp: kp_search/kp_create/kp_link_session/…) into ACP sessions (Grok, Codex) with provenance env (KP_AGENT/KP_MODEL/KP_SESSION). Requires codeBuild.kp.command and codeBuild.kp.root; if either is missing the session starts without kp tools. Independent of the default browser MCP servers — works even with codeBuild.disableDefaultMcpServers or codeBuild.mcpServers: []. A user-supplied 'kp' entry in codeBuild.mcpServers always wins (no duplicate injected)."
        },
        "codeBuild.kp.command": {
          "type": "string",
          "description": "Absolute path to the knowledge-planning CLI entry script (e.g. /Users/me/projects/knowledge-planning/src/cli/index.ts). Spawned as `<node> <this path> mcp` — set the script path, not 'node'."
        },
        "codeBuild.kp.root": {
          "type": "string",
          "description": "Planning store root passed to the kp MCP server as KP_ROOT (e.g. /Users/me/docs/planning)."
        },
        "codeBuild.voice.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Show the Voice bar (dictation, hands-free, Voice Ideation Sessions) in Code Build chat."
        },
        "codeBuild.voice.ttsEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Read assistant replies aloud in hands-free and VIS modes when a turn completes."
        },
        "codeBuild.voice.ttsEngine": {
          "type": "string",
          "enum": [
            "auto",
            "webview",
            "system",
            "off"
          ],
          "default": "auto",
          "description": "TTS engine. 'auto' uses macOS `say` on Darwin and webview speechSynthesis elsewhere. 'webview' always uses the browser synthesizer. 'system' forces host `say` (macOS). 'off' disables TTS."
        },
        "codeBuild.voice.systemVoice": {
          "type": "string",
          "default": "",
          "description": "Optional macOS `say -v` voice name (e.g. Samantha, Daniel). Empty = system default."
        },
        "codeBuild.voice.lang": {
          "type": "string",
          "default": "en-US",
          "description": "BCP-47 language for Web Speech STT and webview TTS (e.g. en-US, ru-RU)."
        },
        "codeBuild.voice.utteranceEndMs": {
          "type": "number",
          "default": 1400,
          "minimum": 400,
          "description": "Silence debounce (ms) after final speech before auto-sending in hands-free / VIS modes."
        },
        "codeBuild.voice.sttEngine": {
          "type": "string",
          "enum": [
            "auto",
            "xai",
            "transcribe",
            "host",
            "webview",
            "off"
          ],
          "default": "auto",
          "description": "Speech-to-text engine. 'auto' prefers 'xai' (Quill-style streaming STT against wss://api.x.ai/v1/stt using the grok CLI login in ~/.grok/auth.json or codeBuild.voice.xaiApiKey; macOS), then 'transcribe' (Amazon Transcribe streaming with your AWS credential chain — the same creds Claude-on-Bedrock uses; macOS), then 'host' (VS Code Speech extension bridge), then webview Web Speech (often blocked by the chat iframe sandbox). Forcing an engine that is unavailable disables STT with guidance."
        },
        "codeBuild.voice.xaiApiKey": {
          "type": "string",
          "default": "",
          "description": "Optional xAI API key for the 'xai' STT engine (console.x.ai). Wins over the grok CLI subscription login; XAI_API_KEY env also works. Leave empty to reuse ~/.grok/auth.json."
        },
        "codeBuild.voice.awsRegion": {
          "type": "string",
          "default": "us-west-2",
          "description": "AWS region for the 'transcribe' STT engine (Amazon Transcribe streaming)."
        },
        "codeBuild.voice.awsProfile": {
          "type": "string",
          "default": "",
          "description": "Optional AWS profile for the 'transcribe' STT engine. Empty = default credential chain (env, SSO, ~/.aws) — the same chain Claude Code on Bedrock uses."
        }
      }
    }
  },
  "scripts": {
    "build": "npm run build:webview && npm run build:host",
    "build:host": "node esbuild.js",
    "build:webview": "vite build",
    "watch:host": "node esbuild.js --watch",
    "watch:webview": "vite build --watch",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p webview-ui/tsconfig.json",
    "test:unit": "node --test --import tsx test/unit/*.test.ts",
    "package": "vsce package --no-dependencies --allow-missing-repository",
    "install-extension": "code --install-extension code-build-vscode-$npm_package_version.vsix --force",
    "ship": "npm run build && npm run package && npm run install-extension",
    "vscode:prepublish": "npm run build"
  },
  "dependencies": {
    "@aws-sdk/client-transcribe-streaming": "^3.700.0",
    "@aws-sdk/credential-providers": "^3.700.0",
    "@unpolarize/agent-memory-core": "^0.0.1",
    "dompurify": "^3.4.7",
    "marked": "^18.0.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@types/vscode": "^1.90.0",
    "@types/ws": "^8.18.1",
    "@vitejs/plugin-react": "^4.3.1",
    "@vscode/vsce": "^3.2.0",
    "esbuild": "^0.21.5",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vite": "^5.3.0"
  }
}
